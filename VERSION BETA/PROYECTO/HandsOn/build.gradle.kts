// Archivo build.gradle.kts de nivel superior (del proyecto)
plugins {
    id("com.android.application") version "8.6.0" apply false // Cambia a la versión que estés usando
    id("com.android.library") version "8.0.2" apply false
    id("org.jetbrains.kotlin.android") version "1.9.10" apply false // Cambia a la versión de Kotlin que estés usando
}

